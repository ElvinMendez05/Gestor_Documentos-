<style>
    .main-screen {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
        font-family: 'Inter', system-ui, -apple-system, sans-serif;
        padding: 20px;
    }

    .glass-card {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        padding: 50px 40px;
        border-radius: 24px;
        box-shadow: 0 20px 40px rgba(0,0,0,0.08);
        text-align: center;
        max-width: 400px;
        width: 100%; /* Evita la distorsión */
        border: 1px solid rgba(255, 255, 255, 0.3);
    }

    /* Animación del icono */
    .icon-box {
        display: flex;
        justify-content: center;
        margin-bottom: 25px;
    }

    .check-mark {
        background: #00d26a;
        width: 80px;
        height: 80px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        box-shadow: 0 10px 20px rgba(0, 210, 106, 0.3);
        animation: scaleIn 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    }

    h1 {
        color: #1e293b;
        font-size: 28px;
        font-weight: 800;
        margin-bottom: 15px;
        letter-spacing: -0.5px;
    }

    p {
        color: #64748b;
        font-size: 16px;
        line-height: 1.6;
        margin-bottom: 30px;
    }

    .divider {
        height: 1px;
        background: #f1f5f9;
        margin-bottom: 30px;
    }

    /* Botón llamativo con icono */
    .btn-modern {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
        background: #007bff;
        color: white;
        text-decoration: none;
        padding: 16px 20px;
        border-radius: 14px;
        font-weight: 600;
        font-size: 16px;
        transition: all 0.3s ease;
        box-shadow: 0 8px 15px rgba(0, 123, 255, 0.25);
    }

        .btn-modern:hover {
            background: #0056b3;
            transform: translateY(-3px);
            box-shadow: 0 12px 20px rgba(0, 123, 255, 0.35);
        }

    /* Animación de entrada */
    @@keyframes scaleIn {
        from {
            transform: scale(0);
            opacity: 0;
        }

        to {
            transform: scale(1);
            opacity: 1;
        }
    }
</style>

<div class="main-screen">
    <div class="glass-card">
        <div class="icon-box">
            <div class="check-mark">
                <svg xmlns="http://www.w3.org/2000/svg" width="50" height="50" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
                    <polyline points="20 6 9 17 4 12"></polyline>
                </svg>
            </div>
        </div>

        <h1>¡Cuenta activada!</h1>
        <p>Tu correo ha sido verificado con éxito. Ya puedes disfrutar de todas nuestras funciones.</p>

        <div class="divider"></div>

        <a asp-action="Login" class="btn-modern">
            <span>Iniciar sesión ahora</span>
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"></line><polyline points="12 5 19 12 12 19"></polyline></svg>
        </a>
    </div>
</div>